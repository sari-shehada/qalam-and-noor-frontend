import 'package:flutter/material.dart';

class GeneralRecordManagementPage extends StatelessWidget {
  const GeneralRecordManagementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Manage General Record Page'),
      ),
    );
  }
}
