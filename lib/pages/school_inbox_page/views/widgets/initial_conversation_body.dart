import 'package:flutter/material.dart';

class InitialConversationBody extends StatelessWidget {
  const InitialConversationBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
